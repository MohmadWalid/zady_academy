@props(['code'])

<div class="inline-flex items-center gap-2 bg-primary-light px-3 py-1.5 rounded-lg border border-primary/20">
    <span class="text-lg font-mono font-bold text-primary">{{ $code }}</span>
</div>
